// ChatMessage model
const mongoose = require('mongoose');

const chatMessageSchema = new mongoose.Schema({
  // TODO: Define chat message schema
});

module.exports = mongoose.model('ChatMessage', chatMessageSchema);